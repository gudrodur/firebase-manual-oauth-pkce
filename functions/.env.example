# OAuth Provider Configuration
OAUTH_ISSUER_URL=https://idp.your-provider.com
OAUTH_CLIENT_ID=your-client-id
OAUTH_CLIENT_SECRET=your-client-secret
OAUTH_REDIRECT_URI=https://your-region-your-project.cloudfunctions.net/handleOAuthCallback

# Firebase Configuration (usually auto-configured in Cloud Functions)
FIREBASE_PROJECT_ID=your-firebase-project
FIREBASE_STORAGE_BUCKET=your-project.appspot.com

# Optional: Custom Claim Configuration
# If your OAuth provider returns a custom claim you want to preserve in Firebase
# (e.g., national ID, employee ID, etc.), set the claim name here
CUSTOM_CLAIM_NAME=national_id
